#include <iostream>
using namespace std;

int main(){
    
    high =max;
    low=1;
    while (low<high){
          mid =low+(int)((high-low)/2);
          if ok(mid) high=mid else low=mid+1;
    }
}
